    - 1️⃣ What is the difference between var, let, and const?
    Ans: var can be redecalared and reassigned hoisted with undefined.
    let cannot be redeclared , can reassigned hoisted but not initialized,
    const cannot be redeclared and reassigned , hoisted but not initialized . 
    - 2️⃣ What is the spread operator (...)?
    Ans: In JavaScript the spead operator means expands an array , object or iterable into individual elements .
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    Ans: map() do tranform mapping elements .
    filter() basically do pick any elements based on condition.
    forEach() do something for each element .
    - 4️⃣ What is an arrow function?
    Ans: arrow function means short function method which is using () => expression without using function keyword .
    - 5️⃣ What are template literals?
    Ans: template literals is that kind of function which is written by backticks(`).


